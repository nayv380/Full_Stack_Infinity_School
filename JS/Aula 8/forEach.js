// Funções Agregadoras - For Each
// Passar uma função, que vai fazer algo para cada valor
// do Array
// Retorno: Nenhum

const numeros = [1, 2, 4, 5, 6, 7, 8];

numeros.forEach((numero) => {
  console.log("Numero", numero);
});
